// Simple test file to verify parser and type checker
mut x: number = 10;
const y: number = 20;

def add(a: number, b: number) -> number {
    return a + b;
}

def main() -> void {
    mut result: number = add(x, y);
    x = result + 5;
}

main();
